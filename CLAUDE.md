# 学内情報共有システム — 09班 CLAUDE.md

## プロジェクト基本情報

| 項目 | 内容 |
|---|---|
| システム名 | 学内情報共有システム（SIT Spot） |
| 対象大学 | 芝浦工業大学（豊洲キャンパス・大宮キャンパス） |
| 班 | 09班（6名） |
| 代表者 | 木幡陽介（yskktech@gmail.com） |
| 仕様書バージョン | Ver. 1.4（2026/7/14, 第五版） |
| 完成目標 | 2026年7月中旬 |

---

## チーム機能分担

| 担当機能 | ユースケース |
|---|---|
| ログイン・プロフィール画面 | UC1〜6（アカウント作成・ログイン・アイコン/ユーザ名変更・投稿削除・ログアウト） |
| 講義内容（授業評価） | UC7〜8 |
| サークル・部活動 | UC9〜10 |
| 過去問 | UC11〜12 |
| 教材売買 | UC13〜14（決済・通知含む） |
| **おすすめスポット（このリポジトリ）** | **UC15〜17** |

> ⚠️ 仕様書 Ver.1.2 以降でUC番号が繰り上がった（旧UC16〜18 → 現UC15〜17）。過去のコミット/コメントの旧番号に注意。

---

## このリポジトリの担当：おすすめスポット機能（UC15〜17）

> Web優先ピボット（2026-07-08）により、仕様書のスマホ前提フロー（カテゴリ→キャンパス選択画面）は
> 廃止し、班共通Webレイアウト（検索画面＋フィルタ）に統合している。以下は実装の実態。

### UC15 おすすめスポットを検索する
- 検索画面でキーワード＋キャンパス／カテゴリのフィルタ（ドロップダウン）で絞り込み → 一覧 → 詳細表示
  - ※ キャンパス選択画面は廃止し、検索画面のフィルタに統合（旧仕様書はキャンパス選択画面フロー）
- 詳細画面: スポット基本情報・地図・他の学生のレビュー

### UC16 おすすめスポットを投稿する
入力項目: スポット名、キャンパス、カテゴリ、位置情報、写真、おすすめポイント（コメント）、星評価
- 位置指定は「店名・住所での検索（Places）＋候補選択」または「地図でピン」の2通り
- 投稿時、投稿者の星評価＋おすすめポイントを**初期レビュー**として登録（averageRating初期化）

### UC17 おすすめスポットのコメントを投稿する
- スポット詳細画面下部の入力欄から、星評価＋コメント（＝レビュー）を入力・送信

### 画面遷移
```
ホーム（ボトムタブ: SPOT）
 └─ スポット検索（キーワード＋キャンパス/カテゴリのフィルタ・＋投稿FAB）
      ├─ スポット詳細（地図・レビュー・コメント入力）
      └─ スポット投稿画面（UC16 / 全画面地図ピッカー・店名検索）
```

---

## 確定技術スタック（要求仕様書 §6 より）

| レイヤー | 技術 | 備考 |
|---|---|---|
| フロントエンド | Flutter SDK (Stable Channel) + Dart | Web・Android・iOS 共通 |
| バックエンド | Firebase Cloud Functions | 言語: TypeScript (ES2020以降) |
| データベース | Cloud Firestore | |
| ファイルストレージ | Firebase Storage | 過去問PDF・商品画像・スポット写真 |
| 地図 | Google Maps API | スポット機能に必須 |
| 認証 | Firebase Auth + 芝浦工大ドメイン制限 | MFA: Authenticatorアプリ OTP |
| 決済 | Stripe API | 教材売買担当が実装（スポット機能では不要） |
| ネットワーク | 大学VPN経由のみバックエンド通信可 | |
| オフライン対応 | PWA（Service Worker）+ Firestore キャッシュ | |

---

## 認証仕様（UC1〜2）

- **登録条件**: 芝浦工業大学ドメイン（@*.shibaura-it.ac.jp 等）のメールアドレスのみ
- **ログイン**: メールアドレス + パスワード → ワンタイムパスワード（MFA）
- ログイン済みユーザーのみ投稿・コメント可能
- 自分の投稿以外は編集・削除不可

---

## UI / デザイン方針（要求仕様書 §4.2 より）

- **6人で共通UIに統一する**（方法は未確定、要チーム合意）
- ボタン形状・配色・余白を全画面で統一
- 主要機能へホームから最大 **3タップ** 以内で到達
- 重要操作（投稿・削除等）は必ず確認ダイアログを表示
- データなし画面は空白にせず「ここから投稿してみましょう」等のガイドを表示
- ボタン押下時にリップル効果を付ける
- 入力種別に応じてキーボードタイプを自動切替（メール→英数字、価格→テンキー）

---

## 品質要件（要求仕様書 §4.1）

| 要件 | 目標値 |
|---|---|
| 通常操作のレスポンス | 1秒以内 |
| コールドスタート | 5秒以内 |
| ウォームスタート | 2秒以内 |
| アニメーション | 60fps維持 |
| クラッシュフリー率 | 99.0%以上 |
| ANR発生率 | 0.47%未満 |
| Firebase通信失敗率 | 1%未満 |
| サービス稼働率 | 99.9%（月間停止43分以内） |
| 画像アップロード | 自動圧縮 1枚あたり1MB以下 |

1秒以上かかる処理はローディングインジケーター必須。

---

## セキュリティ要件（要求仕様書 §4.3）

- 守るべきデータ: 個人情報・投稿内容・購入履歴・閲覧履歴
- アプリ⇔サーバー間は全通信を暗号化
- 氏名・学籍番号等の機密情報と過去問等の公開情報を分離
- アプリ切替時の履歴画面に個人情報を表示しない

---

## 動作環境（要求仕様書 §7）

- **基本**: Webブラウザ動作（レスポンシブ対応）
- **将来**: スマートフォンアプリ展開
- PC: 1920×1080以上 / スマホ: 375×667以上
- 対応OS: Windows 10/11、macOS Big Sur以降、Android 10以降、iOS 14以降
- **大学VPN接続時のみ** Firebaseへの通信可能

---

## Firestoreデータ設計（おすすめスポット担当分・実装準拠）

```
spots/
  {spotId}/
    spotName: string        // スポット名
    campus: string          // "豊洲" | "大宮"（label文字列で保存）
    category: string        // カフェ・飲食店・勉強スペース 等
    description: string?     // おすすめポイント
    latitude: number?       // 緯度
    longitude: number?      // 経度
    imageUrls: string[]     // Firebase Storage URL
    averageRating: number?  // 平均星評価（レビュー追加時にトランザクションで更新）
    reviewCount: number?    // レビュー件数（同上）
    authorUid: string       // 投稿者UID
    createdAt: Timestamp

  {spotId}/reviews/         // ※コメント＝星評価付きレビューとして統一
    {reviewId}/
      spotId: string
      starRating: number    // 星評価（1〜5）
      comment: string
      authorUid: string
      createdAt: Timestamp
```

> 注: 投稿(UC16)時に投稿者の初期レビュー1件が作られ、コメント(UC17)も同じ `reviews` に追加される。
> 星評価は spot ドキュメントに直書きせず、reviews から averageRating/reviewCount を集計する方式。

---

## ディレクトリ構成（方針）

```
lib/
├── main.dart
├── models/
│   ├── spot.dart
│   └── comment.dart
├── screens/
│   ├── campus_select_screen.dart    // キャンパス選択
│   ├── spot_list_screen.dart        // スポット一覧
│   ├── spot_detail_screen.dart      // スポット詳細
│   └── spot_post_screen.dart        // スポット投稿
├── widgets/                         // 共通UIコンポーネント
└── services/
    ├── firestore_service.dart       // Firestore CRUD
    └── storage_service.dart         // 画像アップロード
```

---

## 開発環境セットアップ

```bash
# 依存パッケージのインストール
flutter pub get

# Web で起動（まず動作確認はこちら）
flutter run -d chrome

# Android で起動
flutter run -d android
```

---

## 開発フェーズ

| フェーズ | 内容 | 期限 |
|---|---|---|
| Phase 1 | Flutter Web でスポット検索・投稿の基本動作確認 | 7月上旬 |
| Phase 2 | 地図連携・写真アップロード・コメント機能 | 7月中旬（発表） |
| 将来 | Android/iOS スマホアプリ展開 | 未定 |

---

## 参考：要求仕様書

- ファイル名: `9班 要求仕様書.docx`（Ver. 1.4, 改訂履歴 2026/7/14）
- ユースケース番号: 担当は **UC15（スポット検索）・UC16（スポット投稿）・UC17（コメント投稿）**
